import skillList from '@data/projects'

const ProjectsSection = () => {
  return (
    <>
      {/* !!! 줄바꿈 /n !!!!
      {description.split('\n').map((line, idx) => (
        <p key={idx}>{line}</p>
      ))} */}
    
    </>
  )
}

export default ProjectsSection